<UserControl x:Class="WowConfigCopy.UI.Views.Accounts"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" >
    <Grid>
        <ListView ItemsSource="{Binding Accounts}" Background="{StaticResource BackgroundBrush}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border Style="{StaticResource RoundedListItemStyle}">
                        <StackPanel>
                            <TextBlock Text="{Binding AccountName}" Style="{StaticResource HeadingFontStyle}"/>
                            <TextBlock>
                                <Run Text="{Binding RealmName}"/>
                                <Run Text=" - "/>
                                <Run Text="{Binding RealmRegion}"/>
                            </TextBlock>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>

</UserControl>